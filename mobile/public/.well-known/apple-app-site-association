{
  "applinks": {
    "details": [
      {
        "appIDs": ["WJ7Y8W9WKC.club.pickleague.app"],
        "components": [
          {
            "/": "/privacy*",
            "exclude": true,
            "comment": "Static policy page. Apple review and the App Store listing both link here and expect a readable web page, not an app launch."
          },
          {
            "/": "/landing*",
            "exclude": true,
            "comment": "Marketing page whose whole job is to sell the install. Opening the app instead would hide it from the people it is for."
          },
          {
            "/": "/api/*",
            "exclude": true,
            "comment": "Open Graph card endpoints - crawler surface, never a human destination."
          },
          {
            "/": "/.well-known/*",
            "exclude": true,
            "comment": "This file and its neighbours."
          },
          {
            "/": "/*",
            "?": { "utm_medium": "qr" },
            "exclude": true,
            "comment": "Scanned printed/landing QR codes stay in the browser. Two reasons, and the second is the load-bearing one: the landing card is captioned 'scan to open in browser', and campaign attribution in src/lib/analytics.ts reads window.location.search - it is web-only, so opening the app instead would silently drop the very attribution this QR exists to carry. Must stay ABOVE the catch-all below: components are evaluated in order and the first match wins."
          },
          {
            "/": "/*",
            "comment": "Everything else is a real screen in the app - see the linking config in src/navigation/AppNavigator.tsx, which already maps these paths."
          }
        ]
      }
    ]
  }
}
