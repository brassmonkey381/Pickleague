// Moved to @stockman/rn-foundation. Re-exported here so existing imports
// (`../lib/contacts`) keep working unchanged.
export { loadDeviceContacts } from '@stockman/rn-foundation/platform';
export type { DeviceContact, ContactsResult } from '@stockman/rn-foundation/platform';
