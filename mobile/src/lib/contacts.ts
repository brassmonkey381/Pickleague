// Moved to @just-messin-around/expo-foundation. Re-exported here so existing imports
// (`../lib/contacts`) keep working unchanged.
export { loadDeviceContacts } from '@just-messin-around/expo-foundation/platform';
export type { DeviceContact, ContactsResult } from '@just-messin-around/expo-foundation/platform';
