// Moved to @stockman/rn-foundation/ui (with its .web variant). Re-exported here
// so existing imports (`../components/AppDateTimePicker`) keep working unchanged;
// platform resolution happens at the package's .tsx / .web.tsx pair.
export { AppDateTimePicker as default } from '@stockman/rn-foundation/ui';
